version: "2"

linters:
  default: standard
  exclusions:
    rules:
      # Test helpers intentionally ignore write errors on in-memory buffers
      # and httptest response writers.
      - path: _test\.go
        linters:
          - errcheck

formatters:
  enable:
    - gofmt
